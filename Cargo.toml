[package]
name = "pckr"
version = "0.1.0"
edition = "2021"
description = "tmux session picker TUI with worktree, branch, and merge-status awareness"
license = "MIT"
repository = "https://github.com/jowi-dev/pckr"

[dependencies]
# Unused in this slice (non-TUI core only); declared now so Cargo.lock is
# stable across the slice-2 TUI work. `#![allow(dead_code)]` on the relevant
# items keeps `cargo clippy -D warnings` clean in the meantime.
ratatui = "0.29"
crossterm = "0.28"
